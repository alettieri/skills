interface:
  display_name: "Herdr Worktree Flow"
  short_description: "Run issue worktree implementation loops"
  default_prompt: "Use $herdr-worktree-flow to write the issue brief file, hand off the issue orchestrator, run independent review/fix loops with gpt-4o-mini, use the bundled PR monitor script, push a PR, and continue until merge or close."
